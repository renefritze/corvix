{
    "version": 1,
    "hooks": {
        "sessionStart": [
            {
                "type": "command",
                "bash": "[ -f ~/.agents/hooks/peon-ping/adapters/copilot.sh ] && bash ~/.agents/hooks/peon-ping/adapters/copilot.sh sessionStart || true"
            }
        ],
        "userPromptSubmitted": [
            {
                "type": "command",
                "bash": "[ -f ~/.agents/hooks/peon-ping/adapters/copilot.sh ] && bash ~/.agents/hooks/peon-ping/adapters/copilot.sh userPromptSubmitted || true"
            }
        ],
        "postToolUse": [
            {
                "type": "command",
                "bash": "[ -f ~/.agents/hooks/peon-ping/adapters/copilot.sh ] && bash ~/.agents/hooks/peon-ping/adapters/copilot.sh postToolUse || true"
            }
        ],
        "errorOccurred": [
            {
                "type": "command",
                "bash": "[ -f ~/.agents/hooks/peon-ping/adapters/copilot.sh ] && bash ~/.agents/hooks/peon-ping/adapters/copilot.sh errorOccurred || true"
            }
        ]
    }
}
